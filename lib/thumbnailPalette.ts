// Palette shared by ProjectThumbnail and InsuranceIcon. Reuses the site's own
// theme tokens (styles/globals.css) so the artwork tracks the active theme:
// light chip/dark ink in light mode, charcoal panel/light ink in dark mode.
export const INK = 'var(--color-text)';
export const MUTED = 'var(--color-muted)';
export const PANEL = 'var(--color-bg)';
export const PANEL_BORDER = 'var(--color-border)';
export const BG = 'var(--color-bg-alt)';
